"""Historical-only helper retained for audit continuity.

This script is no longer part of the active validation contract. It must not be
treated as routing authority, CI gate, or default mirror-sync requirement under
the JSON-only active-context path contract.
"""

print("HISTORICAL_ONLY: assert_mirror_sync.py is retired from active validation contract")
