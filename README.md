# ARIS Active Context

- `ACTIVE_CONTEXT_STATE.json` is the only canonical live state.
- `ACTIVE_CONTEXT_SCHEMA.json` is the canonical validation contract.
- `ROADMAP_CANONICAL.md` is the canonical roadmap authority.
- Markdown drift against JSON is a blocking error.

## Current Route

- Latest completed phase: `ARIS Infernus Lab FULL Scope & Attack Taxonomy Planning Gate`
- Previous execution phase: `ARIS Infernus Lab FULL Macroblock Entry Gate`
- Active next phase: `ARIS Infernus Lab FULL 13-Bot Contract Pack Planning Gate`
- Active next phase class: `planning_gate`
- Infernus FULL remains the active macroblock and stays planning-only until an explicit future execution gate authorizes otherwise.

## Canonical Macro Roadmap

```text
Infernus revela.
Purgatorium corrige.
Infernus revalida.
Crisol refina.
Bedrock decide.
```

## Superseded Material

Old R0/F120, F21/F33/C6/C14, Lab Simulation, and Contract Schema Enforcement routes are historical/superseded and must not be used as active roadmap authority.

## Active Artifact

`ARIS_INFERNUS_FULL_ENTRY_GATE.md` is the current entry contract for the active macroblock. It defines purpose, entry criteria, discovery taxonomy, evidence requirements, Purgatorium handoff, and non-authorization boundaries.

`ARIS_INFERNUS_FULL_SCOPE_ATTACK_TAXONOMY_GATE.md` is the planning-only scope and taxonomy contract for the active macroblock. It defines attack families, finding categories, 13-bot discovery roles, severity, evidence requirements, `Purgatorium FULL` handoff fields, and Bedrock impact signals.

## Non-Authorization

Bedrock remains non-executable and product promotion remains blocked. No runtime, frontend, backend, action-runtime, audio, bot, attack, package, dependency, network expansion, external API, real dry-run, real apply, pilot, or commercial execution is authorized by this active context.
