# Weekly Canonical Hygiene Review — 2026-07-06

> Routine: toda segunda-feira 09:00. Revisão somente leitura.
> Fonte de verdade: `ACTIVE_CONTEXT_STATE.json` (HEAD `3129f71`).
> Regras: sem push direto a main; sem apagar arquivos; archive/, excludent/, Project_ARIS intocados.

---

## Resultado: RUÍDO ENCONTRADO

### Resumo executivo

O repositório está canonicamente saudável (locks falsos, next_phase=null), mas acumulou **7 PRs abertas sem merge** e confirma **drift P1 em dois arquivos de mirror** que nenhuma das PRs conseguiu fechar porque nenhuma foi mesclada. Além disso, commits do ACX v2 spec (2026-07-04) introduziram uma divergência nova entre o `phase_id` vivo no JSON e o `phase_id` espelhado nos mirrors.

---

## Estado canônico (JSON, HEAD 3129f71, 2026-07-06)

| Campo | Valor |
|---|---|
| `phase_id` | `DIAGNOSTICO_AUTOMACAO_GATE` |
| `current_phase_id` | `LAPIDARIUM_FASE_6_GUARDA_TRUE` |
| `status` | `lapidarium_fase6_guarda_true_pass` |
| `next_phase` | `null` ✓ |
| `active_next_phase` | `null` ✓ |
| `next_phase_authorized_by_operator` | `false` ✓ |
| `real_apply_authorized` | `false` ✓ |
| `production_authorized` | `false` ✓ |
| `secrets_access_authorized` | `false` ✓ |
| `runtime_integration_allowed` | `false` ✓ |
| `package_installation_authorized` | `false` ✓ |
| `external_llm_api_authorized` | `false` ✓ |

---

## Achados

### A1 — CRÍTICO: 7 PRs abertas, 0 mergeadas (acúmulo progressivo)

Todas as PRs abertas têm 0 reviews e 0 merges. As PRs #3–#7 repetem o mesmo fix porque nenhuma foi mergeada antes do próximo run do doctor:

| PR | Data | Título | Base SHA | Status vs HEAD |
|---|---|---|---|---|
| #1 | 2026-06-24 | mirror-drift README | `ee70087` | ~30 commits stale |
| #2 | 2026-06-29 | weekly hygiene report | `cded6cb` | stale, só relatório |
| #3 | 2026-06-30 | mirror/drift cleanup | `4d16e33` | stale, fix redundante |
| #4 | 2026-07-01 | mirror/drift cleanup | `cda7177` | stale, fix redundante |
| #5 | 2026-07-02 | mirror/drift cleanup | `f6a44bf` | stale, fix redundante |
| #6 | 2026-07-03 | mirror/drift cleanup | `a73d1c2` | stale, fix redundante |
| #7 | 2026-07-04 | mirror/drift cleanup | `b8e4e17` | stale, fix redundante |

O main avançou para `3129f71` (14+ commits desde a base do PR #7). Nenhuma branch de PR está em sync com o HEAD atual.

**Impacto:** O drift que as PRs tentam corrigir permanece em main. Cada novo run do doctor abre uma nova PR redundante. Sem ação do operador, este padrão continuará acumulando.

**Recomendação:** Fechar as PRs #1–#6 como superseded. Reaplicar o fix da PR #7 em cima do HEAD atual (rebase ou novo PR) e mergear.

---

### A2 — P1: Drift confirmado em `CURRENT_STATE.md` (ainda presente em HEAD)

O arquivo `CURRENT_STATE.md` na seção `### next_phase` (antes do `## Historical Appendix`) afirma:

```
next_phase: LAPIDARIUM_FASE_4_REVISAO_CODIGO_GENUINO
next_phase_authorized_by_operator: true
```

O JSON vivo tem `next_phase: null` e `next_phase_authorized_by_operator: false`. Esta é a mesma discrepância que as PRs #3–#7 tentaram corrigir. Permanece sem correção em HEAD.

**Risco:** P1 (mirror contradiz JSON). Sem risco canônico (o JSON ganha sempre), mas cria confusão para leituras rápidas.

---

### A3 — P1: Drift confirmado em `README.md` (ainda presente em HEAD)

O `README.md` contém dois problemas estruturais após o `## Current snapshot` correto (Lapidarium Fase 6):

1. **Segundo bloco `## Current snapshot`** (IF09, sem label `HISTORICAL_ONLY`) posicionado antes do marcador `## Historical Appendix`, lido como estado atual.
2. **Seção `## Estado canônico atual`** exibindo `phase_id: IF09_CLOSURE_MILESTONE_MIRROR_SANITY_PACKET` após o marcador `## Historical Appendix`, sem label `HISTORICAL_ONLY/SUPERSEDED`.

Ambos foram alvo das PRs #3–#7. Permanecem sem correção em HEAD.

---

### A4 — NOVO: Possível drift de `phase_id` após ACX v2 spec (2026-07-04)

Os commits do ACX v2 spec (2026-07-04: "Complete ACX v2 spec", "Freeze ACX v2 spec", "Reconcile ACX R0 decision lock") avançaram `phase_id` no JSON para `DIAGNOSTICO_AUTOMACAO_GATE`, mas os mirrors derivados ainda registram `phase_id: LAPIDARIUM_FASE_6_GUARDA_TRUE`:

| Arquivo | `phase_id` registrado | `phase_id` no JSON |
|---|---|---|
| `CURRENT_STATE.md` | `LAPIDARIUM_FASE_6_GUARDA_TRUE` | `DIAGNOSTICO_AUTOMACAO_GATE` |
| `NEXT_ACTION.md` | `LAPIDARIUM_FASE_6_GUARDA_TRUE` | `DIAGNOSTICO_AUTOMACAO_GATE` |

Nota: No ACX v2 spec, a semântica de `phase_id` pode diferir de versões anteriores (o campo `current_phase_id` continua como `LAPIDARIUM_FASE_6_GUARDA_TRUE`). Recomenda-se verificação pelo operador antes de qualquer atualização de mirror, para confirmar se `phase_id=DIAGNOSTICO_AUTOMACAO_GATE` no JSON representa um avanço de fase ou apenas um campo de candidato.

---

## Itens verificados e limpos

| Item | Status |
|---|---|
| `NEXT_ACTION.md` | LIMPO — reflete Lapidarium Fase 6 guard, próximo passo apenas candidato |
| `ROADMAP_CANONICAL.md` | LIMPO — fases corretamente marcadas CLOSED/CANDIDATE/PLANNED |
| `DECISION_LOCKS.md` | LIMPO (verificado via validações das PRs; arquivo muito grande para leitura inline) |
| `artifacts/active_context/` | LIMPO — 20 arquivos, todos históricos/repair; nenhum artefato órfão novo esta semana |
| Todos os execution locks | `false` ✓ |
| `next_phase` | `null` ✓ |
| `active_next_phase` | `null` ✓ |
| `archive/`, `excludent/` | NÃO TOCADOS |
| `Project_ARIS` | NÃO TOCADO |

---

## Branches abertas (sem PR)

Nenhuma branch órfã. Todas as 9 branches de feature existentes correspondem a PRs abertas.

Branches claude/* sem PR aberta:
- `claude/active-context-revision-laEbr` — sem PR associada (não verificado se é work-in-progress do operador)
- `claude/aris-ac-trans-03-1xxqx` — sem PR associada
- `claude/boot-stale-live-header-repair-zutwh5` — sem PR associada

Essas 3 branches não têm PR aberta visível. Podem ser branches de trabalho em andamento ou orphans. Recomenda-se verificação.

---

## Ações recomendadas ao operador

1. **[ALTA PRIORIDADE]** Fechar PRs #1–#6 como superseded por #7, e então mergear #7 com rebase no HEAD atual (ou aplicar o mesmo fix num PR fresh).
2. **[MÉDIA]** Verificar semântica do `phase_id=DIAGNOSTICO_AUTOMACAO_GATE` no ACX v2 spec; se for avanço de fase real, atualizar mirrors `CURRENT_STATE.md` e `NEXT_ACTION.md`.
3. **[BAIXA]** Verificar branches `claude/active-context-revision-laEbr`, `claude/aris-ac-trans-03-1xxqx`, `claude/boot-stale-live-header-repair-zutwh5` — fechar ou abrir PR.
4. **[BAIXA]** Considerar se o cadenciamento diário do doctor é útil dado que as PRs geradas não estão sendo revisadas no mesmo ciclo.

---

## Garantias desta revisão

- `archive/`, `excludent/`, `Project_ARIS`: não tocados
- `next_phase` / `active_next_phase`: não alterados, permanecem `null`
- Todos os execution locks: permanecem `false`
- Push direto em `main`: não executado
- Arquivos canônicos (`ACTIVE_CONTEXT_STATE.json`, `DECISION_LOCKS.md`, `ROADMAP_CANONICAL.md`): não alterados
- Único arquivo adicionado: este relatório em `artifacts/governance/weekly_hygiene_review/`
