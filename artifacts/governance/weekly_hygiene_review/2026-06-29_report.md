# Weekly Hygiene Review — 2026-06-29

Rotina semanal de higiene canônica (toda segunda-feira, 09:00). Revisão somente leitura: nenhum arquivo foi apagado, `archive/`, `excludent/` e `Project_ARIS` não foram tocados, `next_phase` não foi aberto, e nenhum lock real foi alterado.

## Resultado

`HYGIENE_REPORT` — ruído encontrado, sem correção mecânica óbvia que ainda não estivesse coberta. Nenhuma alteração foi feita aos documentos canônicos. Esta PR contém apenas este relatório.

## Verificações de segurança (todas OK)

- `python3 -m json.tool ACTIVE_CONTEXT_STATE.json` / `ACTIVE_CONTEXT_SCHEMA.json` → válidos.
- `python3 scripts/validate_active_context_state.py` → exit 0, `decision: pass`.
- Scan exato de `"real_apply"`, `"runtime_integration"`, `"secrets_access"`, `"production_authorized"`, `"product_ready"`, `"network"`, `"package_manager"`, `"dependency"` com valor `true` em todo o repositório (`.json`/`.md`) → **zero ocorrências**.
- `next_phase` e `active_next_phase` em `ACTIVE_CONTEXT_STATE.json` → `null` (confirmado antes e depois desta revisão).
- Tail de `DECISION_LOCKS.md` e `ARIS_PHASE_LEDGER.md` concorda com o estado vivo do JSON (sem drift).

## Achados

### 1. PR aberta parada há 5 dias (prioridade alta — ação recomendada: merge)

[`#1 — active-context doctor: daily mirror/drift cleanup 2026-06-24`](https://github.com/MatheusAugDEV/aris-active-context/pull/1), aberta em 2026-06-24, **zero comentários, zero reviews**, `mergeable_state: clean`, 1 arquivo alterado (+1/-1). Corrige uma frase em `README.md` que afirmava em tempo presente `active_next_phase=PURG-01`, contradizendo `ACTIVE_CONTEXT_STATE.json` (`active_next_phase: null`). Confirmado nesta revisão: a linha desatualizada **ainda está presente** em `README.md` (seção "Roadmap macro canônico") na `main` atual — o fix continua válido e necessário.

**Recomendação:** revisar e mergear a PR #1; não há motivo técnico para o atraso.

### 2. Artifacts recentes sem referência canônica

Em `artifacts/active_context/`:

- **`validator_integrity_repair_*` (5 arquivos)** — `_decision.json`, `_hash_manifest.json`, `_no_real_execution_attestation.json`, `_report.md`, `_summary.json`. **Zero referências** em `README.md`, `CURRENT_STATE.md`, `NEXT_ACTION.md`, `DECISION_LOCKS.md`, `ARIS_PHASE_LEDGER.md` ou `CONTEXT_INDEX.md`. O episódio é um check de verificação sem mudança de estado (`restore_performed: false`), o que pode explicar a ausência de entrada no ledger — mas hoje não há nenhum rastro narrativo de que esse episódio existiu.
- **`schema_state_drift_repair_*` (5 arquivos)** — referenciados por caminho completo em `DECISION_LOCKS.md`, porém **ausentes** de `CURRENT_STATE.md`, `NEXT_ACTION.md` e `CONTEXT_INDEX.md` (assimetria em relação ao episódio irmão IF09, que aparece nos quatro lugares).

**Recomendação:** decisão do operador — ou narrar esses dois episódios nos docs canônicos seguindo o padrão já usado para IF09, ou registrar explicitamente que checks "no-op" não geram entrada de ledger (e então este achado fica resolvido por convenção, não por edição).

### 3. Confusão de nomenclatura BenchUIX vs BenchUX — auditoria interna já existente, ainda sem ação (10 dias)

`artifacts/governance/naming_clarity/` já contém uma auditoria própria do repositório (`00_phase_naming_audit.json`, datada de 2026-06-19, citada como última entrada de `DECISION_LOCKS.md`) que identificou exatamente esta ambiguidade: `artifacts/benchuix/` (~280 arquivos, trilha completa BenchUIX) vs `artifacts/benchux/` (2 arquivos: `benchux_pre_route_scope_note.json`, `benchux_route_opening_candidate.json`) — grafias quase idênticas para a mesma família de track, com `display_name_proposed` já sugerido no próprio artifact. Nenhuma consolidação/rename foi aplicada desde então.

**Recomendação:** esta auditoria já é do próprio operador/sistema — apenas sinalizando que está parada há 10 dias sem decisão, mesmo padrão da PR #1.

### 4. `Benchuix_roadmap.md` não citado por nome em `README.md`/`CONTEXT_INDEX.md`

O arquivo é estruturalmente vivo (`ACTIVE_CONTEXT_STATE.json.roadmap_path`, validado por `ACTIVE_CONTEXT_SCHEMA.json` e por `scripts/validate_active_context_state.py` + `tests/test_validate_active_context.py`), mas `README.md` nomeia apenas `ROADMAP_CANONICAL.md` como autoridade de roadmap macro e nunca menciona `Benchuix_roadmap.md` pelo nome. Não é um arquivo órfão (está com carga funcional), mas é uma lacuna documental — um leitor do README não saberia que esse arquivo existe ou importa.

### 5. Nit estrutural em `ARIS_PHASE_LEDGER.md`

Diferente de `CURRENT_STATE.md`, `NEXT_ACTION.md` e `ROADMAP_CANONICAL.md` (que abrem com o estado canônico atual e isolam o histórico depois), `ARIS_PHASE_LEDGER.md` abre com ~48 linhas de blocos históricos P15–P19 repetidos antes de chegar à entrada canônica atual (`## IF09_CLOSURE_MILESTONE_MIRROR_SANITY_PACKET`, linha 49). Não há contradição de conteúdo — apenas ordenação que dificulta leitura rápida do estado vivo.

## Não-achados (checados e OK)

- `CURRENT_STATE.md`, `NEXT_ACTION.md`, `ROADMAP_CANONICAL.md`: seção de topo concorda com `ACTIVE_CONTEXT_STATE.json`; conteúdo histórico devidamente rotulado `HISTORICAL_ONLY` / `SUPERSEDED_BY_...` / `NOT_CURRENT_STATE`.
- `project_mirror/`: nenhum arquivo contradiz o estado vivo sem rótulo de subordinação; nenhum nome de arquivo é quase-duplicata dos docs canônicos de raiz.
- Nenhum outro PR aberto além da #1; nenhum PR fechado/mergeado no histórico do repositório (consistente com a política de não fazer push direto em `main`).

## Itens fora de escopo (conforme regras da rotina)

`archive/`, `excludent/`, `Project_ARIS`, `next_phase` e locks reais não foram alterados nem teriam sido, independentemente dos achados acima.
