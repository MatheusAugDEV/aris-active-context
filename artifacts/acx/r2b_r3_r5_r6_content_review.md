# ACX-R Content Review + Discoverability Proposal

**Scope reviewed**
- R2B: `a7faa9e2186289a619fdb250171a8c836d922286`
- R3: `7274d5429f7683106e14c5cf966da3c2c1341708`
- R5: `718fa46436c90e30eadc700238d93635c54145b2`
- R6: `1881d5eea2743f17b3a7289e0f61dc8a2cb4f89e`

## R2B `a7faa9e`

- `CONFIRMADO`: `event append` / `fold` / `verify-chain` exist and are wired into the CLI.
  - Evidence: `tools/acx.py:98-276` defines `append_event`, `fold`, and `verify_chain`, and `tools/acx.py:279-289` wires `event append`, `fold`, and `verify-chain` into the CLI.
  - Evidence: `tests/test_acx_ledger_shadow.py:49-86` exercises all three commands, including `test_repo_shadow_ledger_verify_chain_passes`, `test_fold_over_golden_fixture_equals_expected_state_byte_for_byte`, and `test_event_append_computes_prev_sha256_correctly`.

- `CONFIRMADO`: the golden fixture test reproduces the state byte-for-byte with diff zero.
  - Evidence: `tests/test_acx_ledger_shadow.py:68-86` asserts `out_path.read_bytes() == EXPECTED_STATE.read_bytes()` in `test_fold_over_golden_fixture_equals_expected_state_byte_for_byte`.
  - Evidence: `tests/test_acx_ledger_shadow.py:75-86` repeats the fold and checks both outputs match `EXPECTED_STATE`.

- `CONFIRMADO`: shadow mode remains shadow-only and does not grant ledger authority over canonical JSON.
  - Evidence: `docs/acx/LEDGER_SPEC.md:1-13` and `docs/acx/LEDGER_SPEC.md:16-24` say during ACX-R2A/R2B, `ACTIVE_CONTEXT_STATE.json` remains the only live canonical source and the ledger is `shadow/audit-only`.
  - Evidence: `tools/acx.py:98-276` only appends/validates/folds the ledger; it reads `ACTIVE_CONTEXT_STATE.json` for baseline hashing but does not mutate canonical JSON.

## R3 `7274d54`

- `CONFIRMADO`: `authority_manifest.json` is generated by script rather than handwritten.
  - Evidence: `tools/acx.py:371-403` defines `_build_authority_manifest(root)`, which walks the repo and classifies paths through `_classify_authority_path`.
  - Evidence: `tools/acx.py:386-388` writes `json.dumps(manifest, sort_keys=True, indent=2, ensure_ascii=False) + "\n"` in `authority_manifest(root, out_path)`.
  - Evidence: `tests/test_acx_authority_manifest.py:66-89` checks `generated_by == "tools/acx.py authority manifest"` and validates `authority_manifest` output through `authority check`.

- `GAP ENCONTRADO`: I did not find a code path proving that the manifest is derived from a named `artifact_integrity_policy` object/ruleset.
  - Evidence: there is no `artifact_integrity_policy` reference in `tools/acx.py`.
  - Evidence: `tools/acx.py:272-368` hard-codes the classification logic in `_classify_authority_path`, not loaded from `ACTIVE_CONTEXT_STATE.json` or `ACTIVE_CONTEXT_SCHEMA.json`.
  - Missing: an explicit read/transform step from `artifact_integrity_policy` into the generated manifest.

- `CONFIRMADO`: `render --check` normalizes comparison inputs before byte comparison.
  - Evidence: `tools/acx.py:293-318` defines `_normalize_render_bytes(data)` and `_compare_rendered_file(root, rel_path)`, which normalize both committed and generated bytes before comparing.
  - Evidence: `tools/acx.py:386-388` emits the manifest deterministically with `sort_keys=True`; the render path itself is also deterministic because `scripts/render_boot.py` derives `BOOT.md` from state and roadmap data without a build timestamp field.

- `CONFIRMADO`: advisory mode reports missing render generators without blocking.
  - Evidence: `tools/acx.py:396-418` prints `render_generator_missing:` and only raises if a real drift is detected.
  - Evidence: `tests/test_acx_authority_manifest.py:168-184` expects `returncode == 0` and `stdout` to contain `render_generator_missing`.

## R5 `718fa46`

- `CONFIRMADO`: `head_before` / `head_after` are captured by the CLI from real `git rev-parse`, not accepted as external input.
  - Evidence: `tools/acx.py:1139-1147` implements `compute_head_sha(root)` via `_git_stdout(root, ["rev-parse", "HEAD"])`.
  - Evidence: `tools/acx.py:1218-1287` uses `compute_head_sha(root)` and stores it as `head_sha`.

- `CONFIRMADO`: anti-replay logic rejects stale bundles when `head_before` differs from the current HEAD at consumption time.
  - Evidence: `tools/acx.py:915-987` checks `current_head is not None and bundle.get("head_before") != current_head` and adds `stale bundle: head_before does not match current HEAD`.
  - Evidence: `tools/acx.py:1003-1026` delegates to `validate_transition_evidence_bundle(...)` and returns `False` if any errors exist.
  - Evidence: `tests/test_acx_evidence_transition_guards.py:105-110` asserts both the error and the `False` transition result.

- `CONFIRMADO`: the anti-PASS lint emits `violation_advisory` records without changing `can_transition`.
  - Evidence: `tools/acx.py:1064-1096` returns dicts with `"event_type": "violation_advisory"`.
  - Evidence: `tests/test_acx_evidence_transition_guards.py:121-145` keeps `can_transition(...) == True` while advisory violations are present.
  - Evidence: the function is pure; it does not mutate ledger or canonical state.

## R6 `1881d5e`

- `CONFIRMADO`: `state_blob_hash` is computed from the real file by the CLI, not accepted as an external parameter of authority.
  - Evidence: `tools/acx.py:1139-1140` hashes bytes from `ACTIVE_CONTEXT_STATE.json`.
  - Evidence: `tools/acx.py:1218-1287` computes `computed_state_hash = compute_state_blob_hash(state_path)` and rejects a mismatched supplied `state_blob_hash`.
  - Evidence: `tools/acx.py:1290-1373` rechecks that the stored hash equals the file hash.

- `CONFIRMADO`: compiler output avoids forbidden decision vocabulary.
  - Evidence: `tools/acx.py:1376-1383` serializes the gate and rejects any forbidden vocabulary hit.
  - Evidence: `tools/acx.py:1440-1535` does the same for packets.
  - Evidence: `tests/test_acx_gate_compiler_operator_packet.py:132-138` assert `contains_forbidden_decision_vocabulary(...) == []`.

## Summary

- Confirmed criteria: 10
- Gaps found: 1

### Gap list

1. R3 manifest generation is script-driven, but I could not confirm derivation from a named `artifact_integrity_policy` source.

## Discoverability proposal

- Proposed field: `acx_meta_track_pointer`
- Intent: let a canonical bootstrapping agent discover published technical tracks without promoting `roadmap_cursor` or conferring phase authority.
- Recommended follow-up: apply only in a separate future schema-migration phase, after explicit operator authorization is recorded.
